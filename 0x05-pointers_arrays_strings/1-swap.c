#include "main.h"

/**
* swap_int - swaps values of two integers
* @b : first pointer
* @a : second pointer
* Return : 0 Always success
*/

void swap_int(int *a, int *b);

{
	int constant = *a;
	*a = *b;
	*a = constant;
}

